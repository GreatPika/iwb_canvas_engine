# Follow-up registry

This registry owns confirmed unresolved work discovered outside an accepted
active Change Contract. Its admission and removal policy is defined in
`docs/planning/README.md`.

Presence in the table means the concern is unresolved. It does not authorize
implementation. Use the next identifier for a new row, increment it in the same
change, and never reuse an identifier after its row is removed.

Next identifier: `FU-001`

| ID | Concern | Evidence | Owner or decision needed |
| --- | --- | --- | --- |
