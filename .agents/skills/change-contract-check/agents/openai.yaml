interface:
  display_name: "Change Contract Check"
  short_description: "Проверка короткого Change Contract перед реализацией"
  default_prompt: "Validate this Change Contract or Contract Blocker. Return a short report with the verdict, all required fixes, and optional improvements only. Do not include passed sections or verbose audit details unless explicitly requested."
