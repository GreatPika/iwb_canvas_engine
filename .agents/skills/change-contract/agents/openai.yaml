interface:
  display_name: "Change Contract"
  short_description: "Draft a traceable change contract"
  default_prompt: "Draft a Change Contract or Contract Blocker for the requested change, preserving source inputs, classification, and decision traceability."
