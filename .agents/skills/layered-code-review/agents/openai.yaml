interface:
  display_name: "Layered Code Review"
  short_description: "Review repository code changes and artifacts"
  default_prompt: "Use $layered-code-review when the user asks for code review of repository changes."
policy:
  allow_implicit_invocation: true
