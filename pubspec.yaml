name: iwb_canvas_engine
description: "Scene-based canvas engine for Flutter: model, rendering, input, JSON serialization."
version: 8.0.0
publish_to: none

environment:
  sdk: ^3.10.4
  flutter: ">=3.38.0"

dependencies:
  characters: ^1.4.1
  flutter:
    sdk: flutter
  meta: ^1.18.0
  path_drawing: ^1.0.1
  vector_graphics: 1.2.3

dev_dependencies:
  crypto: ^3.0.6
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  analyzer: ^12.1.0
  test: ^1.26.3
  vector_graphics_codec: 1.1.13
  vm_service: 15.2.0
  yaml: ^3.1.3
