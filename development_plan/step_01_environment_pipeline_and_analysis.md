language: russian

# Шаг 1. Зафиксировать среду, конвейер и правила анализа

## Цель шага

Сначала убрать неопределённость: этот шаг должен не "на всякий случай ужесточить CI", а зафиксировать, какие именно дыры уже есть в среде и конвейере, а затем закрыть их минимальным набором изменений. Базовая стратегия: сначала диагностировать расхождения между локальной политикой из `AGENTS.md`/`README` и реальным репозиторием, затем закрепить это в `pubspec.yaml`, `analysis_options.yaml`, workflow-файлах и bench-tooling без добавления новой "sync glue" логики.

## Что уже подтверждено по текущему состоянию

1. `analysis_options.yaml` сейчас почти пустой: кроме `flutter_lints` и одного правила он не фиксирует строгую политику анализа и не добавляет проектные запреты.
2. `.github/workflows/ci.yaml` уже запускает основные guardrails, но:
   - perf smoke diff остаётся неблокирующим через `|| true`;
   - `example/test/**` не входит в команду форматирования;
   - нет явной отдельной валидации примера как потребителя только публичного API.
3. `.github/workflows/perf_nightly.yaml` тоже оставляет perf diff неблокирующим через `|| true`, а при отсутствии baseline/current отчёта пишет `skipped` вместо падения job.
4. `tool/bench/run_load_profiles.dart` проверяет только факт наличия хотя бы одного кейса, но не доказывает полноту обязательного perf-набора.
5. `tool/bench/diff_load_profiles.dart` умеет показывать пропущенные кейсы и операции, но сам по себе не формулирует fail-fast политику по обязательным кейсам и допустимым порогам деградации.
6. `example/analysis_options.yaml` уже наследует корневой `analysis_options.yaml`, то есть отдельного "дрейфующего" анализа нет.
7. `example/pubspec.yaml` использует path-зависимость на пакет, поэтому проблема не в отдельной версии движка, а в том, что пример недостаточно жёстко встроен в обязательный конвейер проверок.

## Рекомендуемое решение

Рекомендуемый вариант: не добавлять новый orchestration-слой, а довести до строгого и проверяемого состояния уже существующие механизмы.

Почему это лучший вариант:

1. Он использует уже существующие точки контроля: `flutter analyze`, tool checks, benchmark scripts и GitHub Actions.
2. Он убирает ложную "зелёность" CI, где perf-проверки могут фактически не сработать, но job всё равно завершится успешно.
3. Он сохраняет один источник истины:
   - правила анализа живут в `analysis_options.yaml`;
   - обязательные проверки живут в workflow-файлах;
   - состав perf-набора и критерии валидности живут в `tool/bench/**`.
4. Он чище альтернативы с отдельным meta-скриптом-оркестратором, который дублировал бы команды из CI и создавал бы ещё одну точку расхождения.

## Альтернатива, которую не рекомендуется брать

Не рекомендуется вводить отдельный "bootstrap/check_all.dart" или shell-обёртку, которая повторно описывает все проверки и perf-политику вне CI. Это добавит второй источник истины для команд, порогов и обязательных кейсов, а значит быстро приведёт к дрейфу между локальным запуском и GitHub Actions.

## Итог диагностики и принятые решения

1. `pubspec.yaml`:
   - проблема с `analyzer` подтверждена как диагностическая, но не как блокирующая;
   - текущий диапазон `analyzer: ^8.4.0` проверен локально вместе с `tool/check_guardrails.dart`, `tool/check_import_boundaries.dart`, `tool/check_public_api_surface.dart` и tagged tool-tests;
   - сужать dependency в этом шаге не потребовалось, `pubspec.yaml` остаётся без изменений.
2. `analysis_options.yaml`:
   - проблема подтверждена;
   - в корневой policy включены `strict-casts`, `strict-inference` и `strict-raw-types`;
   - дополнительные lint'ы уровня `avoid_dynamic_calls` и `cast_nullable_to_non_nullable` сознательно не включаются в этом шаге, потому что они требуют непропорциональной массовой чистки по `test/**` и `tool/**`, не добавляя минимально необходимого сигнала для acceptance criteria шага 1.
3. `.github/workflows/ci.yaml`:
   - проблема подтверждена и закрыта;
   - perf smoke diff стал блокирующим;
   - обязательный формат-контур расширен на `example/test/**`;
   - пример добавлен в обязательные `analyze` и `test` шаги.
4. `.github/workflows/perf_nightly.yaml`:
   - проблема подтверждена и закрыта;
   - nightly perf diff стал блокирующим при пропавшем baseline/current отчёте, missing required case/operation и превышении порогов.
5. `tool/bench/run_load_profiles.dart`:
   - проблема подтверждена и закрыта;
   - обязательный набор perf-кейсов теперь валидируется явно против policy.
6. `tool/bench/diff_load_profiles.dart`:
   - проблема подтверждена и закрыта;
   - diff теперь возвращает `pass/fail`, пишет machine-readable failures и валит процесс по policy-нарушениям;
   - policy покрывает и latency, и RSS delta-memory regression thresholds из единого bench policy-модуля, включая absolute fallback для zero-baseline memory cases.
7. `example/pubspec.yaml` и `example/analysis_options.yaml`:
   - отдельной проблемы версии/analysis drift не подтверждено;
   - `example/analysis_options.yaml` продолжает наследовать корневой policy;
   - `example` остаётся на path-зависимости и включён в обязательный проверочный контур.
8. Политика "example использует только публичный API":
   - проблема подтверждена как отдельный контракт;
   - она зафиксирована тестом, который разрешает только `package:iwb_canvas_engine/iwb_canvas_engine.dart` для import'ов пакета из `example/lib/**` и `example/test/**`.

## Что именно менять

### `pubspec.yaml`

[x] Проверить, достаточно ли текущего pin/range для `analyzer` с учётом AST API, которую используют `tool/check_guardrails.dart`, `tool/check_import_boundaries.dart` и связанные tool-tests.
[x] Если AST-совместимость реально зависит от конкретного minor/patch, сузить constraint `analyzer` до диапазона, который гарантированно проходит tool-checks; если текущий диапазон уже стабилен, явно зафиксировать в плане, что проблема не подтверждена и менять файл не нужно.
[x] Добавить сюда только те dev-пакеты, без которых нельзя реализовать новые правила анализа или perf-валидацию; не вводить пакеты "на вырост".

### `analysis_options.yaml`

[x] Перевести файл из минимального состояния в проектную policy-конфигурацию: оставить `flutter_lints` базой и поверх добавить только те правила, которые реально ловят нужные для пакета риски.
[x] Зафиксировать набор правил против опасных приведений, неявных dynamic-flow и других конструкций, которые маскируют ошибки на публичной границе.
[x] Добавить правила или validate-through-tool подход, который делает использование внутренних импортов и утечки изменяемых структур видимыми на CI, не дублируя уже существующие guardrails.
[x] Явно сохранить покрытие `example/**` через общий корневой конфиг; отдельную policy для `example/` не заводить.

### `.github/workflows/ci.yaml`

[x] Сделать perf smoke diff блокирующим: убрать `|| true` и заменить логику `skipped` на fail-fast, если baseline/current отчёт отсутствует или diff невалиден.
[x] Расширить формат-check так, чтобы он охватывал `example/test/**`, а не только `example/lib`.
[x] Явно прогонять проверку примера как потребителя публичного API:
    - либо через `flutter analyze` на общем workspace, если этого достаточно;
    - либо через отдельный шаг, который адресно валидирует `example/lib` и `example/test`.
[x] Сохранить `tool-tests` условными только по trigger list из `AGENTS.md`, но убедиться, что сам trigger list один-в-один совпадает между документом и workflow.
[x] Не добавлять в CI параллельный "супер-скрипт"; править существующие job'ы.

### `.github/workflows/perf_nightly.yaml`

[x] Убрать неблокирующий perf diff и сделать ночной workflow источником достоверного сигнала, а не просто генератором артефактов.
[x] Заваливать job при неполном отчёте, пропавшем обязательном кейсе, пропавшей обязательной операции или превышении заданного порога деградации.
[x] Оставить загрузку артефактов `if: always()`, чтобы даже при падении проверки было что анализировать вручную.

### `tool/bench/**`

[x] В `tool/bench/run_load_profiles.dart` или соседнем вспомогательном модуле формализовать обязательный набор perf-кейсов для `smoke` и `full`.
[x] В `tool/bench/diff_load_profiles.dart` добавить явную интерпретацию результата:
    - что считается невалидным отчётом;
    - какие missing cases/operations считаются ошибкой;
    - какие пороги по времени и памяти валят процесс.
[x] Если для этого нужен отдельный конфигурационный файл с perf-порогами, хранить его рядом с `tool/bench/**` как единственный источник истины, а не размазывать пороги по workflow.
[x] Добавить или обновить tool-tests так, чтобы неполный perf-отчёт, missing case и деградация выше порога воспроизводимо валили проверку.

### `example/**`

[x] Убедиться, что пример использует только `package:iwb_canvas_engine/iwb_canvas_engine.dart` и не импортирует `src/**`; если проблема уже закрыта существующими guardrails, явно зафиксировать это и не плодить второй такой же механизм.
[x] Подключить `example/test/widget_test.dart` к обязательным проверкам форматирования и анализа.
[x] Добавить минимальный smoke-контур для примера только если текущих `flutter analyze` и `flutter test example/test` недостаточно для обнаружения регрессий.

## Конкретизация внедрения по порядку

1. Диагностировать реальное расхождение между документированной политикой проверок и текущими файлами:
   - `pubspec.yaml`
   - `analysis_options.yaml`
   - `.github/workflows/ci.yaml`
   - `.github/workflows/perf_nightly.yaml`
   - `tool/bench/run_load_profiles.dart`
   - `tool/bench/diff_load_profiles.dart`
   - `example/pubspec.yaml`
   - `example/analysis_options.yaml`
2. Для каждого расхождения пометить один из статусов:
   - проблема подтверждена, нужно менять;
   - проблема не подтверждена, шаг плана надо сузить или удалить;
   - нужно дополнительное решение в tool/bench.
3. Сначала править policy и инструменты, потом CI:
   - `analysis_options.yaml`
   - `tool/bench/**`
   - tests для tool/bench
   - workflow-файлы
4. После этого только при необходимости менять `pubspec.yaml`, если выяснится, что без этого AST/tooling не стабилизируются.
5. Завершить шаг только после того, как локальные обязательные команды и CI-логика будут описывать одну и ту же политику.

## Критерии приемки

[x] Для каждого подпункта шага 1 есть явный результат диагностики: проблема подтверждена или опровергнута, без расплывчатого "наверное нужно".
[x] `analysis_options.yaml` больше не является формальным минимальным файлом и реально выражает проектную политику анализа.
[x] CI больше не может завершиться успешно, если perf smoke/nightly отчёт неполный, diff не был проверен или обязательный кейс/операция исчезли.
[x] Обязательные локальные проверки из проектных инструкций и команды в GitHub Actions совпадают по смыслу и покрытию.
[x] `example/` входит в обязательный контур анализа и форматирования; решение по тестам/сборке для примера явно принято и зафиксировано.
[x] Состав обязательных perf-кейсов и пороги деградации находятся в одном месте и автоматически проверяются.
[x] Для новых правил и perf-политики есть тестовое покрытие на уровне `test/tool/**`, если меняется tooling surface.

## Чеклист выполнения

[x] Подтвердить или опровергнуть необходимость сужения `analyzer` в `pubspec.yaml`.
[x] Усилить `analysis_options.yaml` только правилами с высокой полезностью для публичного API и guardrails.
[x] Сделать perf diff в `.github/workflows/ci.yaml` блокирующим.
[x] Сделать perf diff в `.github/workflows/perf_nightly.yaml` блокирующим.
[x] Зафиксировать обязательный набор perf-кейсов и операций в `tool/bench/**`.
[x] Зафиксировать пороги деградации в одном месте, а не в workflow.
[x] Подключить `example/test/**` к обязательному форматированию и анализу.
[x] Проверить и зафиксировать политику "example использует только публичный API".
[x] Добавить/обновить tool-tests для новой perf-policy и fail-fast сценариев.
[x] Сверить trigger list tool-tests между `AGENTS.md` и `.github/workflows/ci.yaml`.
