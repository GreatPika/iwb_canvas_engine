# Launch Screen Assets

This directory contains the iOS launch image assets for the example app.
Replace the image files here, or update them in Xcode via
`ios/Runner.xcworkspace` under `Runner/Assets.xcassets`.
