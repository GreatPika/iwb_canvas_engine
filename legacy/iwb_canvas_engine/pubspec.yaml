name: iwb_canvas_engine
description: "Scene-based canvas engine for Flutter: model, rendering, input, JSON serialization."
version: 5.1.0
homepage: https://github.com/GreatPika/iwb_canvas_engine
repository: https://github.com/GreatPika/iwb_canvas_engine
issue_tracker: https://github.com/GreatPika/iwb_canvas_engine/issues
documentation: https://greatpika.github.io/iwb_canvas_engine/
topics:
  - flutter
  - canvas
  - whiteboard
  - drawing
  - graphics

resolution: workspace

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ^3.10.4
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter
  path_drawing: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  analyzer: ^8.4.0
  test: ^1.26.3
  yaml: ^3.1.3

flutter:
  assets:
    - image/cat.png
